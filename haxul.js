const assert = require("assert")

class Haxul {
    eval(exp) {
        if (isNumber(exr)) return exp;

        throw "Unimplemented";
    }
}


const isNumber = ele => typeof ele === "number"
const isString = ele => typeof ele === "string" &&
